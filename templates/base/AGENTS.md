Inspect the manifest.
Run `taskrail doctor`.
Inspect existing capabilities.
Reuse capabilities when the contract fits.
Create a capability only for small reusable technical functionality.
Keep one-off business logic local.
Change source, never live managed files.
Gate, verify, deploy, and health-check.
