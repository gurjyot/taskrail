{
  "name": "taskrail",
  "version": "2.0.7",
  "private": true,
  "type": "module",
  "description": "Tiny CLI-first framework for safe automation lifecycle management.",
  "bin": {
    "taskrail": "./dist/src/cli.js",
    "taskrail-supervise": "./dist/src/supervise-cli.js",
    "taskrail-heartbeat": "./dist/src/heartbeat-cli.js",
    "taskrail-systemd-sync": "./dist/src/systemd-cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.js",
    "check": "npm run build && node dist/src/cli.js --help && node dist/src/supervise-cli.js --help && node dist/src/heartbeat-cli.js --help && node dist/src/systemd-cli.js --help"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.2"
  }
}
